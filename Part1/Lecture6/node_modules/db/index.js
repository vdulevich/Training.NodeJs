var logger = require("logger")(module);

var phrases;

exports.connect = function(){
    this.phrases = require("./ru");
};

exports.getPhrase = function(name){
    if(!this.phrases[name]){
        throw new Error("Phrase not found: " + name);
    }
    return this.phrases[name];
}

logger.log("Reburied");
