var phrases = require("./ru")

function User(name) {
    this.name = name ? name : "Unknown user";
}

User.prototype.hello = function(){
    console.log("'%s' says: %s", this.name, phrases.Hello);
};


module.exports.User = User;